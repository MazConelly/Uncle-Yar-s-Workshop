; ; section name can be anything as long as it is unique
; [anim_vsk94]
; ; this is the section we want to append to, has to kebab together [faction]_[rank]_[slot]. Slot can be empty, defaults to primary. Can also use _p, _s, _e shorthand for primary, secondary, extra
; loadout_sec = monolith_novice_primary
; ; this is the section of weapons in wep_importer we want to add
; weapons_sec = vsk94_vanilla

; [anim_vsk94_monolith]:anim_vsk94
; loadout_sec = monolith_veteran_primary, monolith_master_primary

; ;-----------------------------------------------------------------------

; [anim_vsk94_csky]:anim_vsk94_monolith
; loadout_sec = csky_master_primary

; ;-----------------------------------------------------------------------

; [anim_vsk94_duty]:anim_vsk94_monolith
; loadout_sec = freedom_veteran_primary, freedom_master_primary

; ;-----------------------------------------------------------------------

; [anim_vsk94_stalker]:anim_vsk94_monolith
; loadout_sec = master_primary, stalker_expert_primary
; ;-----------------------------------------------------------------------

; [anim_vsk94_ecolog]:anim_vsk94_monolith
; loadout_sec = ecolog_merc_primary

; ;-----------------------------------------------------------------------

; [anim_vsk94_killer]:anim_vsk94_monolith
; loadout_sec = killer_veteran_primary, killer_master_primary
; ;-----------------------------------------------------------------------

; [anim_vsk94_greh]:anim_vsk94_monolith
; loadout_sec = greh_experienced_primary, greh_veteran_primary, greh_master_primary
; ;-----------------------------------------------------------------------

; [anim_vsk94_isg]:anim_vsk94_monolith
; loadout_sec = isg_master_primary, isg_primary
